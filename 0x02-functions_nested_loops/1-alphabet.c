#include <stdio.h>

/**
 * print_alphabet - A program that prints alphabet in lowercase
 *
 * Return: Always 0
 */

int main(void)
{

	void print_alphabet(void)

		char ch = 'a';

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		putchar('a');
		putchar('\n');
	}
	return (0);
}
