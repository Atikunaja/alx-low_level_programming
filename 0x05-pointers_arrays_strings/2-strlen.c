#include "main.h"
/**
 * strlen - Find the length of a string
 * @s: string to be counted
 * Return: length of a string
 */
int _strlen(char *s)
{
	int i;
	int j = 0

	for (i = 0; s[i] != '\0'; i++)
		j++;
	return (0);
}
