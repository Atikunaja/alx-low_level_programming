#include "main.h"
#include <string.h>
#include <stdio.h>

/**
 * _strlen - length of a string
 * @s: string to be counted
 * @len: length of a string
 * Return: length of a string
 */

int _strlen(char *s)
{
	int len;
	char str;

	while (*s != '\0')
	{
		str++;
		len += 1;
	}
	return (length);
}
