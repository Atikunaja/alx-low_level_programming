#include "main.h"
/**
 * string_toupper - change all lowercase
 * @n: pointer to upper
 * Return: n
 */
char *string_toupper(char *n)
{
	int i;

	(i, n = 0);
	while (n[i] != '\0')
		n++;
	{
		(n[i] = 'a' && n[i] <= 'z');
			n[i] = n[i] - i;
			i++;
	}
	return (n);
}
