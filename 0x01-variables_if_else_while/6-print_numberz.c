#include <stdio.h>

/**
 * main  - A program that prints all single digit numbers of base ten
 * Return: 0 (Success)
*/

int main(void)
{
	int  digit;

	while (digit <= 10)
	{
		putchar(digit);
		digit++;
	}
		putchar ('\n');

		return (0);
}
